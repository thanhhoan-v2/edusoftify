import type { Route } from "next";

export const HOME_PAGE = "/" as Route;
export const EDIT_COURSES_PAGE = "/edit-courses" as Route;
export const SIGN_UP_PAGE = "/sign-up" as Route;
export const SIGN_IN_PAGE = "/sign-in" as Route;
export const ACCOUNT_SETTINGS_PAGE = "/account-settings" as Route;
